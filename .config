# --- 1. 目标机型设置 (小米 AC2100 / RM2100) ---
CONFIG_TARGET_ramips=y
CONFIG_TARGET_ramips_mt7621=y
CONFIG_TARGET_ramips_mt7621_DEVICE_xiaomi_mi-router-ac2100=y

# --- 2. 必须的插件 (OpenVPN + EasyTier) ---
# OpenVPN 及其 LuCI 界面
CONFIG_PACKAGE_openvpn-openssl=y
CONFIG_PACKAGE_luci-app-openvpn=y

# EasyTier 及其 LuCI 界面 (依赖于你在 diy-part1.sh 添加的 kenzo 源)
CONFIG_PACKAGE_luci-app-easytier=y
# 强制选中 easytier 主程序，防止只编译了界面没编译内核
CONFIG_PACKAGE_easytier=y

# --- 3. 推荐添加的辅助工具 (网页终端) ---
# 因为不能用 SSH，建议加上网页终端 ttyd，方便后续在网页里输入命令调试
CONFIG_PACKAGE_luci-app-ttyd=y

# --- 4. 精简策略 (尽量去掉不用的) ---
# 去掉 Docker (AC2100 跑不动)
# CONFIG_PACKAGE_luci-app-docker is not set
# CONFIG_PACKAGE_dockerd is not set

# --- 5. 编译格式 ---
# 生成 sysupgrade.bin 格式固件 (这是刷机用的)
CONFIG_TARGET_ROOTFS_SQUASHFS=y
